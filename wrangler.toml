name = "contlesas-new"
main = "src/worker.js"
compatibility_date = "2026-03-17"

[assets]
directory = "./dist"

# Non-sensitive environment variables
# Secrets (MAILERLITE_API_KEY) must be set in the Cloudflare dashboard:
#   Workers & Pages > contlesas-new > Settings > Variables and Secrets
# or via CLI: wrangler secret put MAILERLITE_API_KEY
[vars]
MAILERLITE_GROUP_ID = "173612830070670438"
